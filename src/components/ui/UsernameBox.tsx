import type { JSX } from "react";
import type { UsernameBoxProps } from "@/types/components-props";

const UsernameBox = ({ username, isShowCircle }: UsernameBoxProps): JSX.Element => {
    return (

    )
}